// Coding examples



function selectionSort(array) {
  for (const i = 0; i< array.length; i++) {
    smallerElement = array[i];

    for (const j = i; j < array.length; j++) {
      if(smallerElement = array[j])
        smallerElement = array[j];
    }

    // delete array[];

  }
}
